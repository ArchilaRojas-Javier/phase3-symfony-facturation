{% extends 'base.html.twig' %}

{% block title %}Edit Client{% endblock %}

{% block body %}
    <section class="flex h-screen w-full">
        <twig:AsideBar/>
        <main class="flex flex-col w-full p-8 gap-6 text-gray-500 items-center justify-center">
            <div class=' flex flex-col gap-6 '>    
                {{ include('client/_form.html.twig', {'form_title': client.name,
                 'form_label': 'Modifier le client'}) }}
            </div>
        </main>
    </section>
{% endblock %}
